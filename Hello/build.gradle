apply plugin 'java'

sourceSets {
    main {
        java {
            srcDir 'src'
        }

        jar {
            manifest {
                attributes("Main-Class": "Hello")
            }
        }
    }
}